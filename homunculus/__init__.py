"""HOMUNCULUS — an embodied prediction-error agent.

P0 (this milestone) is the harness: a deterministic tick loop over a trivial
world with a random-acting agent, an append-only structured event log, and
seeded replay that reconstructs any tick's state. Belief, perception, surprise,
drives, memory, and the LLM mind arrive in P1+.
"""

__version__ = "0.0.1"
